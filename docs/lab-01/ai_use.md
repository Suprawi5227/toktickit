# Lab 1 — AI Use and Reflection

**LLM/agent used:** Antigravity IDE (Gemini / AI Coding Assistant)

## Selected key prompts (6–10)
| # | Prompt (summarised) | What I did with the result |
|---|---------------------|----------------------------|
| 1 | "ขอคำแนะนำและวิธีตั้งค่าโครงสร้างโปรเจกต์เริ่มต้นสำหรับ Issue 1" | ตรวจสอบและอนุมัติให้ AI รันคำสั่งตั้งค่าโปรเจกต์ (`npm init`) และติดตั้ง Dependencies พื้นฐาน |
| 2 | "แนวทางการพัฒนา API `/api/health` และการเชื่อมต่อกับฝั่ง Frontend สำหรับ Issue 2 คืออะไร" | นำโครงสร้างโค้ดที่ AI แนะนำมาประยุกต์ใช้ในการสร้าง Endpoint และคอมโพเนนต์ React สำหรับตรวจสอบสถานะระบบ |
| 3 | "พบปัญหาติดอยู่ที่หน้าต่าง `COMMIT_EDITMSG` ระหว่างทำ Git Commit ต้องแก้ไขอย่างไร" | ปฏิบัติตามคำแนะนำของ AI โดยการปิดแท็บเพื่อยกเลิกกระบวนการ Commit ที่ไม่สมบูรณ์ |
| 4 | "ช่วยวิเคราะห์และทบทวนโค้ด (Code Review) สำหรับ Pull Request ของเพื่อน (เน้นเรื่อง Schema และ Mock DB)" | นำผลการวิเคราะห์เชิงโครงสร้างจาก AI ไปปรับใช้เป็นข้อเสนอแนะที่สร้างสรรค์ (Constructive Feedback) บน GitHub |
| 5 | "พบข้อผิดพลาด `Cannot GET /` บนหน้าเว็บ เกิดจากสาเหตุใดและแก้ไขอย่างไร" | ทำความเข้าใจเรื่องการแยก Port ระหว่าง Backend (3000) และ Frontend (5173) ตามที่ AI อธิบาย และเข้าใช้งานผ่าน Port ที่ถูกต้อง |
| 6 | "ขอแผนการพัฒนา Issue 4 (ระบบดึงข้อมูล Category) และคำสั่งสำหรับทดสอบ API ผ่าน Terminal" | อนุมัติแผนการเขียนโค้ดดึงข้อมูลผ่าน Prisma และใช้คำสั่ง `Invoke-RestMethod` ที่ AI สร้างให้เพื่อทดสอบความถูกต้องของระบบ |

## Reflection
การให้ข้อมูลบริบทที่ชัดเจน (เช่น ก๊อปปี้รีวิวเพื่อนมาแปะ หรือแคปจอ Error มาให้) ทำให้ AI สามารถช่วยเหลือและร่างคำตอบได้ตรงจุดมากยิ่งขึ้น อย่างไรก็ตาม ในบางครั้งที่ AI อาจจะยังไม่เห็นภาพรวมทั้งหมด (เช่น ตอนที่เพื่อนดันโค้ดขึ้นมาไม่ครบ) เราต้องเข้าไปตรวจสอบโค้ดด้วยตัวเองร่วมกับ AI ด้วย เพื่อหาข้อผิดพลาดที่แท้จริงก่อนนำไปคอมเมนต์ครับ
